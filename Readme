XOR-Convert:

Usage:
XOR-Convert inputfile_1 inputfile_2 outputfile

The XOR operation is applied to the n-th byte of each inputfile and
written to the n-th byte of the outputfile. It should be noted that inputfile_2
must be equal or bigger than inputfile_1.
